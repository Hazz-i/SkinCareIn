services:
  web:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "8888:8888"
    networks:
      - skinsight-net
    env_file:
      - .env
    environment:
      - DB_HOST=mysql_server
      - DB_PORT=${DB_PORT}

networks:
  skinsight-net:
    external: true
